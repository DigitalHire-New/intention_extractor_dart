name: user_intent_classifier
description: High-performance intent classifier using GPT-4o-mini. Features persistent caching, automatic batching, and 94% cost reduction. Classifies job-related messages into JOB_POST, INTERVIEW, or CANDIDATE_SEARCH intents.
version: 3.0.0
homepage: https://github.com/DigitalHire-New/intention_extractor_dart
repository: https://github.com/DigitalHire-New/intention_extractor_dart
issue_tracker: https://github.com/DigitalHire-New/intention_extractor_dart/issues

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http: ^1.2.0
  shared_preferences: ^2.2.2

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.6
